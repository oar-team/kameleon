xionee_install:
 - xionee_checkout_trunk_only:
   - exec_chroot: bash -c "cd /root/ ; svn checkout --username anonsvn --password anonsvn $$xionee_repository/trunk --non-interactive --trust-server-cert ; mv trunk xionee"
 - xionee_checkout_R-branch_only:
   - exec_chroot: bash -c "cd /root/  ; svn checkout --username anonsvn --password anonsvn $$xionee_repository/branches/R-analysis --non-interactive --trust-server-cert"
#  - xionee_full_install:
#    - exec_chroot: bash -c "cd /root/  ; svn checkout --username anonsvn --password anonsvn $$cmdctrl_repository --non-interactive --trust-server-cert"
#    - exec_chroot: bash -c "cd /root/  ; svn checkout --username anonsvn --password anonsvn $$rctrl_repository --non-interactive --trust-server-cert"
#    - exec_chroot: bash -c "cd /root/  ; svn checkout --username anonsvn --password anonsvn $$xionee_repository --non-interactive --trust-server-cert"
#    - exec_chroot: bash -c "cd /root/cmdctrl/ ; ./setup.rb"
#    - exec_chroot: bash -c "cd /root/rctrl/ ; ./setup.rb"

