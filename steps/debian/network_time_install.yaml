network_time_install:
  
  - ntp_install:
    - exec_context: install ntp
  
  - set_timezone:
    - write_file:
      - /etc/timezone
      - Europe/Paris
    - exec_chroot: dpkg-reconfigure -f noninteractive tzdata
