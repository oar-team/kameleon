global:
  workdir_base: /tmp/kameleon
  distrib: rpm
  arch: amd64
  #arch: x84_64
  #arch: i386
#  rpm_distrib_name: centos-4
  rpm_distrib_name: rhel-5
  output_environment_file_system_type: ext3
  network_hostname: kameleon
steps:
  - rpm_check_deps
  - check_deps:
      - rsync
      - building_appliance
      - building_kvm_images
  - bootstrap:
      - rinse_new
  - system_config
  - mount_proc
  - yum:
 #     - fix
      - bootstrap
  - root_passwd
  - kernel_install
  - strip
  - umount_proc

  - build_appliance:
      - clean_udev
      - create_raw_image
      - create_nbd_device
      - mkfs
      - mount_image
      - copy_system_tree
      - install_grub
      - umount_image
      - save_as_raw
      - save_as_vdi
  - clean