# # Make Swap
- create_swap_file:
  - exec: |
    fallocate -l $$swap_size /swap
    chmod 600 /swap
    mkswap /swap
- add_swap_to_fstab:
  - append_file:
    - /etc/fstab
    - /swap none swap defaults 0 0

