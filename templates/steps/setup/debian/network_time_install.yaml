# # Network Time Install

- ntp_install:
  - exec_in: |
      DEBIAN_FRONTEND=noninteractive apt-get -y --force-yes install ntp

- set_timezone:
  - write_file:
    - /etc/timezone
    - $$timezone
  - exec_in: dpkg-reconfigure -f noninteractive tzdata
